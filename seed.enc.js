/* Platzhalter – wird von  tools/encrypt-seed.mjs  mit deinen
 * AES-256-GCM-verschluesselten Buchhaltungsdaten ueberschrieben.
 * Solange null: leere Buchhaltung / Ersteinrichtung.
 *   node tools/encrypt-seed.mjs        (fragt nach der Passphrase)
 *   git add seed.enc.js && git commit -m "Daten" && git push
 */
window.__SEED__ = null;
